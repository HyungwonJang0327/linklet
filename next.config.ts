import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['lh3.googleusercontent.com', 'https://lh3.googleusercontent.com', 'linklet-image.s3.ap-northeast-2.amazonaws.com'],
  },
};

export default nextConfig;
